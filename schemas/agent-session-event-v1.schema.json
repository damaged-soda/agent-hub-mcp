{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-hub.local/schemas/agent-session-event-v1.schema.json",
  "title": "Agent Session Event v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "provider",
    "native_session_id",
    "sequence",
    "kind",
    "occurred_at",
    "data",
    "provenance"
  ],
  "properties": {
    "schema_version": { "const": 1 },
    "provider": { "enum": ["claude", "codex", "kimi", "opencode"] },
    "native_session_id": {
      "oneOf": [
        { "type": "string", "minLength": 1, "maxLength": 512 },
        { "type": "null" }
      ]
    },
    "sequence": { "type": "integer", "minimum": 0 },
    "kind": {
      "enum": [
        "session",
        "context",
        "message",
        "turn-start",
        "turn-end",
        "model-call",
        "tool-call",
        "tool-result",
        "error",
        "rate-limit"
      ]
    },
    "occurred_at": {
      "oneOf": [{ "type": "string", "minLength": 1 }, { "type": "null" }]
    },
    "event_ref": {
      "type": "string",
      "pattern": "^agenthub://session/v1/(claude|codex|kimi|opencode)/[A-Za-z0-9][A-Za-z0-9._%:-]{0,1535}/event/e1_[A-Za-z0-9_-]{43}$"
    },
    "data": { "type": "object" },
    "truncation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["truncated", "fields"],
      "properties": {
        "truncated": { "const": true },
        "fields": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["path", "kind"],
            "properties": {
              "path": { "type": "string" },
              "kind": { "enum": ["string", "array", "object", "depth"] },
              "original_chars": { "type": "integer", "minimum": 0 },
              "original_bytes": { "type": "integer", "minimum": 0 },
              "retained_chars": { "type": "integer", "minimum": 0 },
              "original_count": { "type": "integer", "minimum": 0 },
              "retained_count": { "type": "integer", "minimum": 0 }
            }
          }
        }
      }
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["stage", "source", "native_type"],
      "properties": {
        "stage": {
          "enum": ["requested", "launched", "observed", "inferred", "unknown"]
        },
        "source": {
          "enum": [
            "live-stream",
            "native-transcript",
            "agenthub-request",
            "agenthub-command"
          ]
        },
        "native_type": { "type": "string" }
      }
    }
  }
}
